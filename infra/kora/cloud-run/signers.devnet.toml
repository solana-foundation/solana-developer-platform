# Kora Signer Configuration for SDP Devnet (Cloud Run)

[signer_pool]
strategy = "round_robin"

[[signers]]
name = "sdp_devnet_fee_payer"
type = "gcp_kms"
key_name_env = "KORA_GCP_KMS_KEY_NAME"
public_key_env = "KORA_GCP_KMS_PUBLIC_KEY"
weight = 1
