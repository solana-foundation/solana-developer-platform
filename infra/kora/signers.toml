# Kora Signer Configuration for SDP Local Development
# Uses a memory signer with a keypair from environment variable

[signer_pool]
strategy = "round_robin"

[[signers]]
name = "sdp_fee_payer"
type = "memory"
private_key_env = "SIGNER_PRIVATE_KEY"
weight = 1
