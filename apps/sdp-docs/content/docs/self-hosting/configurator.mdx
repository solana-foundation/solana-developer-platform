---
title: Configurator
description: Generate a complete .env for a self-hosted Solana Developer Platform, entirely in your browser.
---

Fill in the fields below and download a ready-to-use `.env`. Everything runs in
your browser — nothing is sent anywhere. Drop the file next to `compose.yml`, then
run `docker compose up -d`.

<EnvConfigurator />
